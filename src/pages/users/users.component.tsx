import React, { JSX } from 'react';
import classes from './users.component.module.scss';

export const Users: React.FC = (): JSX.Element => {
  return <div className={classes.container}>users</div>;
};
