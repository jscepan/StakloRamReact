import React, { JSX } from 'react';
import classes from './password-reset.component.module.scss';

export const PasswordReset: React.FC = (): JSX.Element => {
  return <div className={classes.container}>password-reset</div>;
};
