import React, { JSX } from 'react';
import classes from './outcomes.component.module.scss';

export const Outcomes: React.FC = (): JSX.Element => {
  return <div className={classes.container}>outcomes</div>;
};
