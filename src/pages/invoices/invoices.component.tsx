import React, { JSX } from 'react';
import classes from './invoices.component.module.scss';

export const Invoices: React.FC = (): JSX.Element => {
  return <div className={classes.container}>Invoices</div>;
};
