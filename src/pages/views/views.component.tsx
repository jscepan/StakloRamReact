import React, { JSX } from 'react';
import classes from './views.component.module.scss';

export const Views: React.FC = (): JSX.Element => {
  return <div className={classes.container}>views</div>;
};
