import React, { JSX } from 'react';
import classes from './settings.component.module.scss';

export const Settings: React.FC = (): JSX.Element => {
  return <div className={classes.container}>settings</div>;
};
