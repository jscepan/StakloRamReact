import React, { JSX } from 'react';
import classes from './invoices.component.module.scss';

export const UserCreateEdit: React.FC = (): JSX.Element => {
  return <div className={classes.container}>user-create-edit</div>;
};
