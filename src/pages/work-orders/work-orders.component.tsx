import React, { JSX } from 'react';
import classes from './work-orders.component.module.scss';

export const WorkOrders: React.FC = (): JSX.Element => {
  return <div className={classes.container}>work-orders</div>;
};
